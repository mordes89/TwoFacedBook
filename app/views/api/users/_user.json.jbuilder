json.extract! user, :id, :email, :first_name, :last_name, :cover_photo, :profile_photo, :created_at, :address, :originally_from, :joinedTFB
# json.photoUrl url_for(@user.photo) if @user.photo.attached? 
